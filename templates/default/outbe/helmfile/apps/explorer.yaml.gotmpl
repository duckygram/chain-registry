app:
  ingress:
    gateways:
      internal:
        hostnames:
          - "explorer.{{ .Values.environment }}.{{ .Values.systemName }}.{{ .Values.ingress.baseDomain }}"
#      public:
#        hostnames:
#          - "node.{{ .Values.environment }}.{{ .Values.systemName }}.in.{{ .Values.ingress.baseDomain }}"